%h2
  .row-fluid
    .text-left.span8
      Hop:
      = @hop.name
    .text-right.span4
      = link_to('Edit', admin_edit_regular_hop_path(@hop), class: 'btn btn-info')
      - if @hop.daily
        = link_to 'Back', admin_daily_hops_path, class: 'btn btn-info'
      - else
        = link_to 'Back', admin_regular_hops_path, class: 'btn btn-info'
      = link_to('Destroy', admin_hop_path(@hop), method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure?' })
.row-fluid
  .span4
    = image_tag @hop.logo.url, class: 'img-polaroid', style: 'max-width: 400px; max-height: 400px'
  .span8
    %table.hops-form
      %tr
        %td
          %b
            Name:
        %td
          = @hop.name
      %tr
        %td
          %b
            Time start:
        %td
          = @hop.time_start
      - unless @hop.daily
        %tr
          %td
            %b
              Time end:
          %td
            = @hop.time_end
        %tr
          %td
            %b
              Producer:
          %td
            - if @hop.producer
              = @hop.producer.first_name
              = @hop.producer.last_name
        %tr
          %td
            %b
              Producer contact:
          %td
            - if @hop.producer
              = @hop.producer.contact
        %tr
          %td
            %b
              Producer Phone:
          %td
            - if @hop.producer
              = @hop.producer.phone
        %tr
          %td
            %b
              Producer Email:
          %td
            - if @hop.producer
              = @hop.producer.email
        %tr
          %td
            %b
              Code:
          %td
            = @hop.code
        %tr
          %td
            %b
              Price:
          %td
            = @hop.price
        %tr
          %td
            %b
              Jackpot:
          %td
            = @hop.jackpot
        %tr
          %td
            %b
              Special event:
          %td
            = @hop.event
%hr
.row-fluid
  .span6
    %h4
      Tasks:
  .span6.text-right
    %button.btn.btn-info{onclick: 'loadForm(' + @hop.id.to_s + ', "' + new_admin_hop_task_path + '", "task-modal")'}
      New Hop Task
#tasksTable
  = render partial: 'admin/hop_tasks/tasks'

#task-modal.modal.hide.fade{tabindex: '-1', role: 'dialog', 'aria-labelledby' => 'myModalLabel', 'aria-hidden' => 'true'}
  .modal-header
    %button{ type: 'button', class: 'close', 'data-dismiss' => 'modal', 'aria-hidden' => true}
      ×
    %h3#myModalLabel
      New Hop Task:
  .modal-body

%hr
.row-fluid
  .span6
    %h4
      Ads:
  .span6.text-right
    %button.btn.btn-info{onclick: 'loadForm(' + @hop.id.to_s + ', "' + new_admin_ad_path + '", "ad-modal")'}
      New Ad
#adsTable
  = render partial: 'admin/ads/list'

#ad-modal.modal.hide.fade{tabindex: '-1', role: 'dialog', 'aria-labelledby' => 'myModalLabel', 'aria-hidden' => 'true'}
  .modal-header
    %button{ type: 'button', class: 'close', 'data-dismiss' => 'modal', 'aria-hidden' => true}
      ×
    %h3#myModalLabel
      New Ad:
  .modal-body


%hr
%h4
  Hoppers:
- if @hoppers.blank?
  List has no rows
- else
  %table.table.table-condensed
    %tr
      %th
        First Name
      %th
        Last Name
      %th
        User Name
      %th
    - @hoppers.each do |user|
      %tr
        %td
          = link_to user.first_name, user_path(:id => user.id)
        %td
          = user.last_name
        %td
          = user.user_name
        %td
          = image_tag user.avatar.url, class: 'img-polaroid', style: 'height: 110px'


