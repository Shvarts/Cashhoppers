<p id="notice"><%= notice %></p>



<p>
  <b>Name:</b>
  <%= @hop.name %>
</p>

<p>
  <b>Time start:</b>
  <%= @hop.time_start %>
</p>

<p>
  <b>Time end:</b>
  <%= @hop.time_end %>
</p>

<p>
  <b>Producer:</b>
  <%= @hop.producer_id %>
</p>

<p>
  <b>Producer contact:</b>
  <%= @hop.producer_contact %>
</p>

<p>
  <b>Contact phone:</b>
  <%= @hop.contact_phone %>
</p>

<p>
  <b>Contact email:</b>
  <%= @hop.contact_email %>
</p>

<p>
  <b>Hop code:</b>
  <%= @hop.hop_code %>
</p>

<p>
  <b>Hop price:</b>
  <%= @hop.hop_price %>
</p>

<p>
  <b>Jackpot:</b>
  <%= @hop.jackpot %>
</p>

<p>
  <b>Hop items:</b>
  <%= @hop.hop_items %>
</p>
<%= @hop.id %>

  <% @hop.close %>



<%= link_to 'Edit', edit_admin_hop_path(@hop) %> |
<%= link_to 'Back', admin_hops_path %>


<h2>Tasks</h2>
<% @hop.hop_tasks.each do |hop_task| %>
<table>
  <tr>
    <td><strong>text:</strong></td>
    <td><strong>price:</strong></td>
    <td><strong>picture:</strong></td>
    <td></td>
    <td></td>
    <td></td>

  <tr>

  <tr>
    <td>

      <%= hop_task.text_for_hop %>
    </td>
    <td>

      <%= hop_task.hop_task_price %>
    </td>
    <td>

      <%= hop_task.hop_picture.url %>
    </td>

    <td><%= link_to 'Show', admin_hop_task_path(hop_task.id)  %></td>
    <td><%= link_to 'Edit', edit_admin_hop_task_path(hop_task) %></td>
    <td><%= link_to 'Destroy',  [:admin, hop_task], :confirm => 'Are you sure?', :method => :delete %></td>

  </tr>
</table>

<% end %>

<%= form_tag admin_hop_tasks_path, :multipart => true do %>

    <div class="field">
      <%= label_tag :text_for_hop %><br />
      <%= text_area_tag :text_for_hop %>
    </div>

    <div class="field">
      <%= label_tag :hop_task_price %><br />
      <%= number_field_tag :hop_task_price %>
    </div>

    <div class="field">

      <%= hidden_field_tag :hop_id, @hop.id %>
    </div>


    <div class="field">
      <%= label_tag :hop_picture %><br />
      <%= file_field_tag :hop_picture %><br />

      <div class="actions">
        <%= submit_tag %>
      </div>
<% end %>


<h2>Ad</h2>

<% @hop.hop_ads.each do |hop_ad| %>
    <table>
      <tr>
        <td><strong>text:</strong></td>
        <td><strong>price:</strong></td>
        <td><strong>picture:</strong></td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>

      <tr>

      <tr>
        <td>

          <%= hop_ad.ad_name %>
        </td>
        <td>

          <%= hop_ad.phone %>
        </td>
        <td>

          <%= hop_ad.email %>
        </td>

        <td><%= link_to 'Show',  admin_hop_ad_path(hop_ad.id)%></td>
        <td><%= link_to 'Edit', edit_admin_hop_ad_path(hop_ad)  %></td>
        <td><%= link_to 'Destroy',  [:admin, hop_ad], :confirm => 'Are you sure?', :method => :delete  %></td>

      </tr>
    </table>

<% end %>


<%= form_tag admin_hop_ads_path(:hop_id=>@hop.id) , :multipart => true do %>

    <div class="field">
      <%= label_tag :ad_name %><br />
      <%= text_area(:hop_ad, :ad_name, :rows=>5) %>
    </div>

    <div class="field">
      <%= label_tag :contact %><br />
      <%= text_field(:hop_ad,:contact) %>
    </div>

    <div class="field">
      <%= label_tag :email%><br />
      <%= email_field(:hop_ad,:email) %>
    </div>

    <div class="field">
      <%= label_tag :phone%><br />
      <%= text_field(:hop_ad,:phone) %>
    </div>

    <div class="field">
      <%= label_tag :price%><br />
      <%= number_field(:hop_ad, :price) %>
    </div>

    <div class="field">
      <%= label_tag :hop_ad_picture%><br />
      <%= file_field(:hop_ad, :hop_ad_picture) %>
    </div>

    <div class="field">

      <%= radio_button(:hop_ad,:ad_type, "sp")%>
      <%= label_tag :ad_type,  "SP" %>
      <%= radio_button(:hop_ad, :ad_type, "rofl") %>
      <%= label_tag :ad_type,  "ROFL" %>
      <%= radio_button(:hop_ad, :ad_type, "ropu")%>
      <%= label_tag :ad_type, "ROPU" %>

    </div>





      <div class="actions">
        <%= submit_tag %>
      </div>
<% end %>
<div class="actions">
  <td><%= button_to 'Close project', admin_close_path %></td>
</div>

