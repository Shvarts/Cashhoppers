%ul.nav.nav-tabs.nav-stacked.non-print
  %li
    = link_to 'Applications', admin_applications_path if can? :manage, :all
  %li
    = link_to 'Messages',  admin_messages_email_tool_path if can? :manage, :all
  %li
    = link_to 'Hoppers',  admin_hoppers_find_hopper_path if can? :manage, :all
  %li
    = link_to 'Hops', admin_regular_hops_path if !User.user?(current_user)
  %li
    = link_to 'Ads', admin_ads_path if !User.user?(current_user)

  %li
    = link_to 'Manage Users', admin_users_index_path if can? :manage, :all
  %li
    =link_to 'FAQ', admin_asks_path if can? :manage, :all
